{
  "name": "anna-bliver-fan-hub",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "test": "npm test --prefix backend",
    "build": "npm run build --prefix frontend",
    "seed:demo": "npm run seed:demo --prefix backend"
  }
}
